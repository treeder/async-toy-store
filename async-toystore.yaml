asyncapi: '2.0.0-rc1'
id: 'urn:hello-world-app'
info:
  title: Hello world application
  version: '0.1.0'
servers:
  - url: localhost:4222
    protocol: nats
    description: Nats broker
channels:
  hello:
    publish:
      message:
        payload:
          type: string
          pattern: '^hello .+$'
